@tailwind base;
@tailwind components;
@tailwind utilities;

html {
  @apply h-full;
}

body {
  @apply min-h-full bg-gray-100;
}

@media (prefers-color-scheme: dark) {
  body {
    @apply bg-gray-900;
  }
}
